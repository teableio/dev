# Rename this file to .env.local

# GitHub OAuth App
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret

# NextAuth
AUTH_SECRET=generate_with_openssl_rand_base64_32
AUTH_URL=https://dev.teable.ai

# GCP Configuration
GCP_PROJECT_ID=teable-666
GCP_ZONE=asia-east2-a
GCP_MACHINE_TYPE=n2-standard-8
GCP_IMAGE_FAMILY=teable-dev

# Optional: GCP credentials (not needed if running on GCP with proper service account)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

